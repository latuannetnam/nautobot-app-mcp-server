# Embedded MCP endpoint removed in v1.2.0 (Phase 12).
# The standalone MCP server runs on port 8005 via start_mcp_server.py / start_mcp_dev_server.py.
# No URL routing here — see __init__.py: urls = []
