---
phase: "18-graphql-only-mode"
plan: "18-05"
type: execute
wave: 3
depends_on:
  - "18-01"
  - "18-02"
files_modified:
  - "CLAUDE.md"
  - "nautobot-mcp-skill/nautobot_mcp_skill/SKILL.md"
autonomous: true
requirements:
  - "GQLONLY-06"
must_haves:
  truths:
    - "CLAUDE.md Gotchas table contains NAUTOBOT_MCP_GRAPHQL_ONLY row"
    - "SKILL.md contains GraphQL-Only Mode section"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Documentation of NAUTOBOT_MCP_GRAPHQL_ONLY in Gotchas table"
      contains: "NAUTOBOT_MCP_GRAPHQL_ONLY"
    - path: "nautobot-mcp-skill/nautobot_mcp_skill/SKILL.md"
      provides: "SKILL.md GraphQL-Only Mode section"
      contains: "GraphQL-Only Mode"
---

<objective>
Document NAUTOBOT_MCP_GRAPHQL_ONLY in CLAUDE.md (Gotchas table) and SKILL.md (new section). Per D-10 and D-11 from CONTEXT.md.
</objective>

<execution_context>
@/home/latuan/Local_Programming/nautobot-project/nautobot-app-mcp-server/.planning/phases/18-graphql-only-mode/18-CONTEXT.md
@/home/latuan/Local_Programming/nautobot-project/nautobot-app-mcp-server/CLAUDE.md
</execution_context>

<context>
@/home/latuan/Local_Programming/nautobot-project/nautobot-app-mcp-server/CLAUDE.md
@/home/latuan/Local_Programming/nautobot-project/nautobot-app-mcp-server/nautobot-mcp-skill/nautobot_mcp_skill/SKILL.md

D-10 specifies the CLAUDE.md entry as one row in the Gotchas table:
```
| GraphQL-only mode hides non-GraphQL tools | Set/unset NAUTOBOT_MCP_GRAPHQL_ONLY env var and restart |
```

D-11 specifies the SKILL.md entry as a short section covering:
- Env var name
- What it does (only graphql_query + graphql_introspect visible and callable)
- How to enable (restart with NAUTOBOT_MCP_GRAPHQL_ONLY=true)
- No workflow examples needed
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add NAUTOBOT_MCP_GRAPHQL_ONLY row to CLAUDE.md Gotchas table</name>
  <files>CLAUDE.md</files>
  <read_first>/home/latuan/Local_Programming/nautobot-project/nautobot-app-mcp-server/CLAUDE.md</read_first>
  <action>
Read the Gotchas table section in CLAUDE.md and add this exact row:

```
| GraphQL-only mode hides non-GraphQL tools | Set/unset NAUTOBOT_MCP_GRAPHQL_ONLY env var and restart |
```

Place it after the existing Gotchas table rows. The table format is:
```
| Issue | Fix |
|---|---|
| existing row 1 | ... |
| existing row 2 | ... |
```

Add the new row after the last existing row. The exact content should match D-10 verbatim.
</action>
  <verify>
<automated>grep -n "NAUTOBOT_MCP_GRAPHQL_ONLY" /home/latuan/Local_Programming/nautobot-project/nautobot-app-mcp-server/CLAUDE.md</automated>
  </verify>
  <done>CLAUDE.md Gotchas table contains row: "| GraphQL-only mode hides non-GraphQL tools | Set/unset NAUTOBOT_MCP_GRAPHQL_ONLY env var and restart |"</done>
</task>

<task type="auto">
  <name>Task 2: Add GraphQL-Only Mode section to SKILL.md</name>
  <files>nautobot-mcp-skill/nautobot_mcp_skill/SKILL.md</files>
  <read_first>/home/latuan/Local_Programming/nautobot-project/nautobot-app-mcp-server/nautobot-mcp-skill/nautobot_mcp_skill/SKILL.md</read_first>
  <action>
Read SKILL.md to find the best location for a new "GraphQL-Only Mode" section. Add a section heading and 2-3 paragraphs covering:

1. **Env var name:** `NAUTOBOT_MCP_GRAPHQL_ONLY`
2. **What it does:** When set to `true`, the MCP server exposes exactly 2 tools: `graphql_query` and `graphql_introspect`. All other tools (10 core read tools + 3 session tools) are hidden from the manifest and blocked at call time.
3. **How to enable:** Set `NAUTOBOT_MCP_GRAPHQL_ONLY=true` and restart the MCP server. Without the env var, all 15 tools behave identically to the default mode.

Example section:
```markdown
## GraphQL-Only Mode

Set `NAUTOBOT_MCP_GRAPHQL_ONLY=true` to restrict the MCP server to GraphQL tools only.

When active, only `graphql_query` and `graphql_introspect` are visible in the tool manifest and callable. All other tools are hidden from the manifest and blocked at call time (calling them returns a `ToolNotFoundError`).

Without this env var (default), the server behaves normally with all 15 tools available.
```

Place this section after the existing "GraphQL Tools" section (if present) or in a logical location near the top after basic setup.
</action>
  <verify>
<automated>grep -n "GraphQL-Only Mode\|NAUTOBOT_MCP_GRAPHQL_ONLY" /home/latuan/Local_Programming/nautobot-project/nautobot-app-mcp-server/nautobot-mcp-skill/nautobot_mcp_skill/SKILL.md</automated>
  </verify>
  <done>SKILL.md contains "GraphQL-Only Mode" section heading and describes NAUTOBOT_MCP_GRAPHQL_ONLY=true behavior</done>
</task>

</tasks>

<verification>
- CLAUDE.md grep finds "NAUTOBOT_MCP_GRAPHQL_ONLY" 
- SKILL.md grep finds "GraphQL-Only Mode" and "NAUTOBOT_MCP_GRAPHQL_ONLY"
- Both documentation files contain the correct content per D-10 and D-11
</verification>

<success_criteria>
CLAUDE.md Gotchas table and SKILL.md both document NAUTOBOT_MCP_GRAPHQL_ONLY per D-10 and D-11
</success_criteria>

<output>
After completion, create `.planning/phases/18-graphql-only-mode/18-05-SUMMARY.md`
</output>
