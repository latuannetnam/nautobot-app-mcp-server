---
phase: 15-introspection-permissions
plan: "15.2"
subsystem: testing
tags: [graphql, permissions, mock, testing, auth]

# Dependency graph
requires:
  - phase: 14-graphql-tool-scaffold
    provides: graphql_query MCP tool, _sync_graphql_query helper, GraphQLQueryHandlerTestCase base
provides:
  - GQL-13 permission enforcement tests covering AnonymousUser → empty/error and AuthenticatedUser → data
affects: [14-graphql-tool-scaffold, 16-security-hardening]

# Tech tracking
tech-stack:
  patterns: [mock-based unit tests, lazy import patching, ValueError → structured error dict]

key-files:
  modified:
    - nautobot_app_mcp_server/mcp/tests/test_graphql_tool.py

key-decisions:
  - "Patches nautobot.core.graphql.execute_query (not graphql_tool module) because _sync_graphql_query uses lazy import inside function body)"
  - "user=None for anonymous path (not AnonymousUser()) — triggers ValueError from execute_query)"
  - "MagicMock(formatted={...}) for authenticated path — mirrors ExecutionResult.formatted structure"

patterns-established:
  - "Pattern: @patch at source module for lazy-imported functions"
  - "Pattern: ValueError side_effect → assertIsNone(data) + assertIn('errors', result)"
  - "Pattern: MagicMock.return_value.formatted → assertNotEqual(data, [])"

requirements-completed: [GQL-13]

# Metrics
duration: 5min
completed: 2026-04-16
---

# Phase 15.2: Introspection & Permissions Summary

**GQL-13 permission enforcement tests: AnonymousUser → empty/error, AuthenticatedUser → non-empty data via mock patches of nautobot.core.graphql.execute_query**

## Performance

- **Duration:** 5 min
- **Started:** 2026-04-16
- **Completed:** 2026-04-16
- **Tasks:** 1 (two test methods)
- **Files modified:** 1

## Accomplishments
- `test_anonymous_user_empty_query_results`: patches `nautobot.core.graphql.execute_query` with `ValueError` side_effect; asserts `user=None` yields `data=None` and structured error dict
- `test_authenticated_user_normal_results`: mocks `ExecutionResult.formatted`; asserts superuser returns non-empty device list

## Task Commits

The two test methods were committed atomically with the plan revision in the same base commit (f3165e6):

1. **test(15.2): add GQL-13 permission enforcement tests** — `f3165e6` (test + plan commit)

**Plan metadata:** `f3165e6` (refactor(15): revise plan after checker review)

## Files Created/Modified
- `nautobot_app_mcp_server/mcp/tests/test_graphql_tool.py` — added 2 test methods to `GraphQLQueryHandlerTestCase`

## Decisions Made

- Patches `nautobot.core.graphql.execute_query` at its definition site (not the graphql_tool module namespace) because `_sync_graphql_query` uses a lazy import (`import ... inside function body`), so the name is not bound in graphql_tool's namespace at decoration time
- `user=None` for anonymous path (not `AnonymousUser()`) — matches how the Phase 14 existing test uses `AnonymousUser()`, which also patches `execute_query`; the plan-specified `user=None` is the correct path to trigger `ValueError`
- Uses `MagicMock(formatted={...})` for authenticated path — mirrors the `ExecutionResult.formatted` structure returned by the real function

## Deviations from Plan

None — plan executed exactly as written. The two test methods were already committed in the base commit `f3165e6` (plan revision commit); verified identical to plan specification.

## Issues Encountered

- Poetry venv corruption on host (`Permission denied: 'CACHEDIR.TAG'`) — ran tests inside Docker container instead
- Pre-existing test failures in `test_session_tools.py` (6 failures, unrelated to GQL-13) — confirmed present before any changes

## Next Phase Readiness

- GQL-13 tests pass (7/7 graphql_tool tests OK)
- Ready for Phase 15.3 (graphql_introspect tool implementation)
- Full suite: 98 tests, 6 pre-existing failures (scope/visibility unrelated to Phase 15)

---
*Phase: 15-introspection-permissions | Plan: 15.2*
*Completed: 2026-04-16*
