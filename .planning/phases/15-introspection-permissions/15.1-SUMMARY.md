---
phase: 15-introspection-permissions
plan: "15.1"
subsystem: api
tags: [graphql, fastmcp, nautobot, mcp, introspection, graphene]

# Dependency graph
requires: []
provides:
  - "_graphql_introspect_handler async tool (GQL-08, GQL-09)"
  - "GraphQL schema SDL via graphql-core print_schema()"
  - "Auth-gated introspection (raises ValueError for anonymous)"
affects: [15.2, 15.3]

# Tech tracking
tech-stack:
  added: [graphql-core print_schema, graphene_django.settings]
  patterns:
    - "Async tool handler wrapping sync helper via sync_to_async(thread_sensitive=True)"
    - "Auth gate at MCP tool level (raises ValueError → FastMCP tool error)"
    - "Lazy import of graphene_django.settings inside sync helper"

key-files:
  modified:
    - nautobot_app_mcp_server/mcp/tools/graphql_tool.py
    - nautobot_app_mcp_server/mcp/tests/test_graphql_tool.py

key-decisions:
  - "Used ValueError(\"Authentication required\") raised in async handler — FastMCP converts to tool error"
  - "Schema accessed via graphene_settings.SCHEMA.graphql_schema (same path as execute_query)"
  - "No user argument passed to _sync_graphql_introspect — introspection is not permission-filtered"

patterns-established:
  - "Async handler pattern: get_user_from_request → None check → sync_to_async helper"
  - "output_schema=None not passed in @register_tool decorator (project auto-derives from type hints)"
  - "Test pattern: patch get_user_from_request and _sync helper at module level"

requirements-completed: [GQL-08, GQL-09]

# Metrics
duration: 47min
completed: 2026-04-15
---

# Plan 15.1: graphql_introspect MCP Tool — Summary

**GraphQL introspection MCP tool added: returns Nautobot schema as SDL string, auth-gated via ValueError**

## Performance

- **Duration:** 47 min
- **Started:** 2026-04-15T21:15:00Z
- **Completed:** 2026-04-15T22:02:00Z
- **Tasks:** 1 (plan 15.1 = single tool)
- **Files modified:** 2

## Accomplishments
- `graphql_introspect` MCP tool registered via `@register_tool(name="graphql_introspect", tier="core")`
- `_graphql_introspect_handler` async handler: auth gate → `sync_to_async(thread_sensitive=True)` → `_sync_graphql_introspect`
- `_sync_graphql_introspect` sync helper: uses `graphql.print_schema(graphene_settings.SCHEMA.graphql_schema)`
- Auth required: raises `ValueError("Authentication required")` for anonymous callers
- 4 unit tests added: `test_introspect_returns_sdl_string`, `test_introspect_sdl_valid`, `test_introspect_raises_on_anonymous`, `test_auth_required_resolves_user`
- All 9 tests pass (5 existing + 4 new)

## Task Commits

1. **Task 1: graphql_introspect tool implementation + unit tests** - `1ce0f41` (feat)
   - Added `_graphql_introspect_handler` and `_sync_graphql_introspect` to `graphql_tool.py`
   - Added `GraphQLIntrospectHandlerTestCase` with 4 tests to `test_graphql_tool.py`

**Plan metadata:** `f3165e6` (refactor(15): revise plan after checker review)

## Files Created/Modified

- `nautobot_app_mcp_server/mcp/tools/graphql_tool.py` — appended `graphql_introspect` handler and `_sync_graphql_introspect` helper
- `nautobot_app_mcp_server/mcp/tests/test_graphql_tool.py` — appended `GraphQLIntrospectHandlerTestCase` with 4 tests

## Decisions Made

- Auth gate uses `ValueError` raised in async handler (not dict return) because `graphql_introspect` returns `str`, not `dict`
- `_sync_graphql_introspect` takes no `user` argument — introspection returns schema structure (not data), so permission filtering does not apply
- Lazy import of `graphene_django.settings` inside sync helper avoids Django setup issues at module load time
- `graphene_django.settings.graphene_settings.SCHEMA.graphql_schema` used (same path confirmed in RESEARCH.md:920)

## Deviations from Plan

None - plan executed exactly as written.

### Auto-fixed Issues

**1. [Code Quality - Ruff] Docstring blank-line spacing in handler**
- **Found during:** Task 1 (ruff check)
- **Issue:** Missing blank line after `Returns:` section (D410) and before `Raises:` section (D411)
- **Fix:** Added blank lines after `Returns:` and before `Raises:` in handler docstring
- **Files modified:** `nautobot_app_mcp_server/mcp/tools/graphql_tool.py`
- **Verification:** `ruff check` → All checks passed
- **Committed in:** `1ce0f41` (part of task commit)

**2. [Code Quality - Ruff] Import order in _sync_graphql_introspect**
- **Found during:** Task 1 (ruff check)
- **Issue:** `from graphql import print_schema` before `from graphene_django.settings import graphene_settings` (I001)
- **Fix:** Reordered to `graphene_django` first (third-party stdlib ordering)
- **Files modified:** `nautobot_app_mcp_server/mcp/tools/graphql_tool.py`
- **Verification:** `ruff check --fix` → All checks passed
- **Committed in:** `1ce0f41` (part of task commit)

**3. [Code Quality - Ruff] Unused `user` variable in test_introspect_sdl_valid**
- **Found during:** Task 1 (ruff check)
- **Issue:** `_get_or_create_superuser()` called but result unused (F841)
- **Fix:** Removed the unused call — no user fixture needed for this mock-only test
- **Files modified:** `nautobot_app_mcp_server/mcp/tests/test_graphql_tool.py`
- **Verification:** `ruff check --fix` → 0 errors remaining
- **Committed in:** `1ce0f41` (part of task commit)

---

**Total deviations:** 3 auto-fixed (3 code quality)
**Impact on plan:** All auto-fixes were code-quality improvements with no functional impact.

## Issues Encountered

- **Worktree git confusion:** Initial `git reset --hard` was run against repo root instead of worktree. Resolved by resetting the worktree via `git -C <worktree-path>` and re-applying the edit.
- **Docker container test stale:** Container volume-mounts repo root (not worktree). Repo-root file had old content; copied updated files from worktree to repo root before running container tests.
- **Venv root permission:** `.venv` at repo root created as root (permission denied). Recreated via `rm -rf .venv && poetry env use python3` from worktree directory.

## Next Phase Readiness

- `graphql_introspect` tool registered and tested (GQL-08 ✓, GQL-09 ✓)
- `output_schema=None` is passed by `register_all_tools_with_mcp()` (not in decorator — correct per project pattern)
- Tool appears in `tool_registry.json` (plugin `ready()` auto-regenerates on startup)
- Phase 15.2 (unit tests for auth + introspect) can proceed immediately

---
*Phase: 15-introspection-permissions*
*Completed: 2026-04-15*
