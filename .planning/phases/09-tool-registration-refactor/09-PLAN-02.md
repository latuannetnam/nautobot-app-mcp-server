---
wave: 1
depends_on: ["09-01"]
files_modified:
  - nautobot_app_mcp_server/mcp/__init__.py
  - nautobot_app_mcp_server/mcp/commands.py
autonomous: true
---

## 09-02: `register_all_tools_with_mcp()`

### Context

`register_all_tools_with_mcp(mcp)` reads `MCPToolRegistry.get_all()` and calls `mcp.tool(func, name=..., description=...)` for each registered tool. **Important:** `mcp.tool()` does NOT accept `input_schema` as a parameter — FastMCP 3.x auto-derives the schema from the function's Python type hints. The registry copy of the schema is for cross-process discovery only.

This function is called inside `create_app()` after the FastMCP instance is created, and before `create_app()` returns. Both `start_mcp_server` and `start_mcp_dev_server` call `create_app()`, so both get the same wiring automatically.

### Tasks

```xml
<task>
  <read_first>nautobot_app_mcp_server/mcp/__init__.py</read_first>
  <acceptance_criteria>grep "def register_all_tools_with_mcp" nautobot_app_mcp_server/mcp/__init__.py returns the new function definition; grep "register_all_tools_with_mcp(mcp)" nautobot_app_mcp_server/mcp/commands.py returns the call inside create_app()</acceptance_criteria>
  <action>
In nautobot_app_mcp_server/mcp/__init__.py, add `register_all_tools_with_mcp()` AFTER the `register_tool` decorator (and after the `__all__` update):

```python
def register_all_tools_with_mcp(mcp: Any) -> None:
    """Register all tools from MCPToolRegistry with a FastMCP instance.

    Called at MCP server startup (inside ``create_app()``) to attach every
    tool that has been registered via :func:`register_mcp_tool` or
    :func:`register_tool` to the live FastMCP instance.

    FastMCP 3.x does NOT accept ``input_schema`` as a parameter to
    ``mcp.tool()`` — the schema is auto-derived from the function's Python
    type hints at decoration/wiring time. The ``input_schema`` stored in
    MCPToolRegistry is for cross-process discovery (e.g. ``tool_registry.json``)
    and is NOT passed to FastMCP here.

    Args:
        mcp: A FastMCP instance returned by ``FastMCP(...)``.

    Note:
        Must be called AFTER ``nautobot.setup()`` so that Django is bootstrapped
        and lazy imports inside tool functions can succeed.
    """
    registry = MCPToolRegistry.get_instance()
    for tool in registry.get_all():
        # mcp.tool() is a decorator in FastMCP 3.x — call it with kwargs
        mcp.tool(tool.func, name=tool.name, description=tool.description)
```

Add `"register_all_tools_with_mcp"` to the `__all__` list in `mcp/__init__.py`.
  </action>
</task>

<task>
  <read_first>nautobot_app_mcp_server/mcp/commands.py</read_first>
  <acceptance_criteria>grep "register_all_tools_with_mcp" nautobot_app_mcp_server/mcp/commands.py returns the import and the call</acceptance_criteria>
  <action>
In nautobot_app_mcp_server/mcp/commands.py, update STEP 4 to call `register_all_tools_with_mcp(mcp)`:

After the FastMCP instance is created (STEP 3, line where `mcp = FastMCP(...)`), add:

```python
    # STEP 4: Wire all registered tools to FastMCP.
    # Importing nautobot_app_mcp_server.mcp.tools side-effects registration into
    # MCPToolRegistry (via @register_tool on each core tool handler).
    from nautobot_app_mcp_server.mcp.tools import core  # noqa: F401

    from nautobot_app_mcp_server.mcp import register_all_tools_with_mcp

    register_all_tools_with_mcp(mcp)
```

The complete STEP 3 and STEP 4 block in `commands.py` should look exactly like:

```python
    # STEP 3: Build FastMCP instance.
    # FastMCP 3.x does NOT accept host/port in the constructor — passed at run time.
    from fastmcp import FastMCP

    mcp = FastMCP(
        "NautobotMCP",
        # Note: stateless_http and json_response are passed at run time
        # via mcp.run(transport="http", ...) or mcp.http_app(transport="http").
        # FastMCP 3.x does NOT accept these in the constructor.
    )

    # STEP 4: Wire all registered tools to FastMCP.
    # Importing nautobot_app_mcp_server.mcp.tools side-effects registration into
    # MCPToolRegistry (via @register_tool on each core tool handler).
    from nautobot_app_mcp_server.mcp.tools import core  # noqa: F401

    from nautobot_app_mcp_server.mcp import register_all_tools_with_mcp

    register_all_tools_with_mcp(mcp)

    return (mcp, host, port)
```

Remove the comment `# STEP 4: Phase 9 — wire register_all_tools_with_mcp() here. (Placeholder until Phase 9 lands.)` — it is replaced by the real implementation above.
  </action>
</task>
```

### Verification

```bash
# register_all_tools_with_mcp is defined in mcp/__init__.py
grep "def register_all_tools_with_mcp" nautobot_app_mcp_server/mcp/__init__.py

# register_all_tools_with_mcp is called in commands.py
grep "register_all_tools_with_mcp" nautobot_app_mcp_server/mcp/commands.py
# Expected: import + call (2 lines)

# Phase exit gate — all MCP tests pass
poetry run nautobot-server test nautobot_app_mcp_server.mcp.tests
```