---
wave: 1
depends_on: ["09-01"]
files_modified:
  - nautobot_app_mcp_server/mcp/tools/core.py (no code changes)
autonomous: true
---

## 09-04: Confirm All 10 Core Tools — `async def` + `sync_to_async`

### Context

Phase 5 established the `async def + sync_to_async(thread_sensitive=True)` pattern in `core.py`. P2-04 is a **confirmatory** requirement — verifying the pattern is correctly implemented, with no code changes required.

Each of the 10 tools follows this chain:
1. `async def _X_handler(ctx: ToolContext, ...)` — the FastMCP tool function (registered with `@register_tool`)
2. `await sync_to_async(query_utils._sync_X, thread_sensitive=True)(...)` — wraps the sync ORM call

### Tasks

```xml
<task>
  <read_first>nautobot_app_mcp_server/mcp/tools/core.py</read_first>
  <acceptance_criteria>grep -c "^async def _" nautobot_app_mcp_server/mcp/tools/core.py returns exactly 10; grep -c "sync_to_async(query_utils._sync_" nautobot_app_mcp_server/mcp/tools/core.py returns exactly 10</acceptance_criteria>
  <action>
Audit nautobot_app_mcp_server/mcp/tools/core.py to confirm the Phase 5 async pattern is correctly implemented for all 10 tools.

**Verification checklist** (no code changes):

1. **Handler function signature:** Each tool handler is `async def` with `ToolContext` as first parameter:
   - `_device_list_handler(ctx: ToolContext, limit: int = 25, cursor: str | None = None)`
   - `_device_get_handler(ctx: ToolContext, name_or_id: str)`
   - `_interface_list_handler(ctx: ToolContext, device_name: str | None = None, limit: int = 25, cursor: str | None = None)`
   - `_interface_get_handler(ctx: ToolContext, name_or_id: str)`
   - `_ipaddress_list_handler(ctx: ToolContext, limit: int = 25, cursor: str | None = None)`
   - `_ipaddress_get_handler(ctx: ToolContext, name_or_id: str)`
   - `_prefix_list_handler(ctx: ToolContext, limit: int = 25, cursor: str | None = None)`
   - `_vlan_list_handler(ctx: ToolContext, limit: int = 25, cursor: str | None = None)`
   - `_location_list_handler(ctx: ToolContext, limit: int = 25, cursor: str | None = None)`
   - `_search_by_name_handler(ctx: ToolContext, query: str, limit: int = 25, cursor: str | None = None)`

2. **sync_to_async call:** Every handler calls `await sync_to_async(query_utils._sync_X, thread_sensitive=True)(...)`:
   ```python
   return await sync_to_async(query_utils._sync_X, thread_sensitive=True)(
       user=user, ...kwargs...
   )
   ```

3. **ToolContext import:** `from fastmcp.server.context import Context as ToolContext` is present.

4. **No Django model imports at module level:** Confirm `from nautobot.dcim.models import` or `from nautobot.ipam.models import` are NOT at module level in core.py (models are only accessed via `query_utils._sync_*` which handles its own imports).

If all 10 tools match the pattern, document the confirmation and mark complete. If any discrepancy is found, fix it inline.

**This task is read-only — confirm the pattern is correct and document the confirmation in the task result.**
  </action>
</task>
```

### Verification

```bash
# All 10 handlers are async def
grep -c "^async def _" nautobot_app_mcp_server/mcp/tools/core.py
# Expected: 10

# All 10 use sync_to_async with thread_sensitive=True
grep -c "sync_to_async(query_utils._sync_" nautobot_app_mcp_server/mcp/tools/core.py
# Expected: 10

# ToolContext is imported
grep "ToolContext" nautobot_app_mcp_server/mcp/tools/core.py

# No module-level Django model imports in core.py
grep "from nautobot.dcim.models import\|from nautobot.ipam.models import" nautobot_app_mcp_server/mcp/tools/core.py
# Expected: no output (models accessed via query_utils only)

# Phase exit gate — all MCP tests pass
poetry run nautobot-server test nautobot_app_mcp_server.mcp.tests
```