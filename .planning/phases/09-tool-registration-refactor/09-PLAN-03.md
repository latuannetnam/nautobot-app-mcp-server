---
wave: 1
depends_on: ["09-01"]
files_modified:
  - nautobot_app_mcp_server/__init__.py
autonomous: true
---

## 09-03: Plugin `ready()` Generates `tool_registry.json`

### Context

`tool_registry.json` enables cross-process discovery. It is written by the Nautobot plugin process (via `NautobotAppConfig.ready()`) and read by the MCP server process at startup. `post_migrate` is NOT used — `post_migrate` never fires in the MCP server process (Phase 8 runs `django.setup()` directly, not via `nautobot-server`).

The `ready()` method: (1) calls `super().ready()` first so Nautobot registers URL patterns, (2) imports `mcp.tools` to trigger side-effect registration in `MCPToolRegistry`, (3) writes `tool_registry.json`.

### Tasks

```xml
<task>
  <read_first>nautobot_app_mcp_server/__init__.py</read_first>
  <acceptance_criteria>grep "tool_registry.json" nautobot_app_mcp_server/__init__.py returns the file write; grep "post_migrate" nautobot_app_mcp_server/__init__.py returns zero matches; grep '"input_schema": tool.input_schema' nautobot_app_mcp_server/__init__.py returns the schema inclusion line</acceptance_criteria>
  <action>
Replace the entire `ready()` method in nautobot_app_mcp_server/__init__.py with the following implementation:

```python
def ready(self) -> None:
    """Initialize the MCP server app.

    1. Call super().ready() FIRST so Nautobot registers our urls.py via
       NautobotAppConfig.ready() → plugin_patterns.append(...).
    2. Import mcp.tools to trigger side-effect registration of all core tools
       in MCPToolRegistry.
    3. Write tool_registry.json to the package directory for cross-process
       discovery by the standalone MCP server.

    Note:
        Does NOT use post_migrate. post_migrate never fires in the MCP server
        process (Phase 8 runs django.setup() directly, not nautobot-server).
        The MCP server reads tool_registry.json at startup instead.
    """
    super().ready()  # Registers URL patterns (MUST be first)

    import json  # noqa: F401
    import os  # noqa: F401

    import nautobot_app_mcp_server.mcp.tools  # noqa: F401

    from nautobot_app_mcp_server.mcp.registry import MCPToolRegistry

    registry = MCPToolRegistry.get_instance()

    # Build payload for tool_registry.json
    # Excludes 'func' (not JSON-serializable); input_schema included for
    # client-side visibility without calling the server.
    tools = registry.get_all()
    payload = [
        {
            "name": tool.name,
            "description": tool.description,
            "tier": tool.tier,
            "app_label": tool.app_label,
            "scope": tool.scope,
            "input_schema": tool.input_schema,
        }
        for tool in tools
    ]

    # Write to package directory via __file__ (resolves correctly for both
    # installed packages and editable dev installations).
    package_dir = os.path.dirname(__file__)
    json_path = os.path.join(package_dir, "tool_registry.json")

    with open(json_path, "w") as f:
        json.dump(payload, f, indent=2)
```

Also remove the `_on_post_migrate` static method and the `post_migrate.connect(...)` call in `ready()`. The entire `post_migrate` wiring block must be removed.
  </action>
</task>
```

### Verification

```bash
# ready() contains tool_registry.json generation
grep "tool_registry.json" nautobot_app_mcp_server/__init__.py

# post_migrate wiring is removed
grep "post_migrate" nautobot_app_mcp_server/__init__.py
# Expected: no output

# JSON file path uses os.path.dirname(__file__)
grep "os.path.dirname(__file__)" nautobot_app_mcp_server/__init__.py

# input_schema is included in the JSON payload
grep '"input_schema": tool.input_schema' nautobot_app_mcp_server/__init__.py

# Phase exit gate — all MCP tests pass
poetry run nautobot-server test nautobot_app_mcp_server.mcp.tests
```