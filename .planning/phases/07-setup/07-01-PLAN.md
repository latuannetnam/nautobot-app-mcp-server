---
wave: 1
depends_on: []
requirements:
  - P0-01
files_modified:
  - pyproject.toml
autonomous: true
---

# Plan 07-01: Add uvicorn explicit dependency

## Task: Add `uvicorn >= 0.35.0` to `[tool.poetry.dependencies]`

### <read_first>

- **`pyproject.toml`** — the file being modified; current dependency list starts at line 30

### <action>

Add `uvicorn = ">=0.35.0"` to the `[tool.poetry.dependencies]` section, immediately after `fastmcp = "^3.2.0"` on line 36. The placement keeps it in the `# MCP server layer` block alongside its primary consumer (`fastmcp`).

```toml
# MCP server layer
fastmcp = "^3.2.0"
uvicorn = ">=0.35.0"
```

Insert on its own line after line 36. No other changes to the file.

### <acceptance_criteria>

- [ ] `grep "^uvicorn" pyproject.toml` matches `uvicorn = ">=0.35.0"` in the `[tool.poetry.dependencies]` section (not under a group)
- [ ] `grep "uvicorn" pyproject.toml` returns exactly one match under `[tool.poetry.dependencies]`
- [ ] The constraint is lower-bound only (`>= 0.35.0`, no upper bound) — FastMCP's transitive constraint applies naturally
- [ ] `poetry lock` (or `poetry check`) validates the dependency without error
