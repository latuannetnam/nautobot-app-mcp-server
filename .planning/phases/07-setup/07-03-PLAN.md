---
wave: 3
depends_on: []
requirements:
  - P0-03
files_modified:
  - docs/admin/upgrade.md
autonomous: true
---

# Plan 07-03: Document `--workers 1` requirement in upgrade.md

## Task: Add `--workers 1` warning section to `docs/admin/upgrade.md`

### <read_first>

- **`docs/admin/upgrade.md`** — the file being modified; current content is a minimal skeleton (lines 1–11)

### <action>

Append a new top-level section to `docs/admin/upgrade.md` immediately after the existing `## Upgrade Guide` block (after line 11). The section uses a Material for MkDocs `!!! warning` admonition block.

```markdown
## Worker Process Requirement

!!! warning "Single Worker Required"
    The MCP server **must** be run with `--workers 1` (the default for uvicorn).

### Rationale

The MCP server stores session state in-memory. Running with multiple workers
(`--workers N` where `N > 1`) causes sessions to be lost when requests are routed
to different worker processes, breaking the progressive tool discovery feature.

### Production Deployment

For production deployments using systemd, the default uvicorn worker count (1)
is already correct. If explicitly setting workers:

```ini
[Service]
ExecStart=/opt/nautobot/venv/bin/nautobot-server start_mcp_server --workers 1
```

### Development

The development server (`start_mcp_dev_server`) uses `uvicorn.run(reload=True)`
which always runs with a single worker.

### Future: Horizontal Scaling

For deployments requiring `--workers N` where `N > 1`, a Redis session backend
is required to share session state across processes. This is planned for v2.0.
```

### <acceptance_criteria>

- [ ] `grep "workers" docs/admin/upgrade.md` matches `--workers 1` (the requirement flag)
- [ ] `grep "in-memory" docs/admin/upgrade.md` matches `in-memory` (explains the root cause)
- [ ] `grep "progressive tool discovery" docs/admin/upgrade.md` matches the feature name (context for why sessions matter)
- [ ] `grep "v2.0" docs/admin/upgrade.md` matches a deferred promise for Redis sessions (sets expectations)
- [ ] The admonition block uses `!!! warning` syntax (Material for MkDocs — verifiable by rendering with `poetry run invoke mkdocs`)
- [ ] `grep "Single Worker Required" docs/admin/upgrade.md` matches the admonition title
